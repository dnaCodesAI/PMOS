# Plugin package — abc-bank-pm-os

Upload **`abc-bank-pm-os.zip`** in Claude Cowork:

**Customize → Personal plugins → Upload plugin**

If `.plugin` upload fails, use the `.zip` file (same contents).

## Rebuild

From the repo root:

```powershell
.\package-plugin.ps1
```
